- pipeline.id: all_users
  pipeline.workers: 1
  path.config: "/usr/share/logstash/pipeline/logstash.conf"

- pipeline.id: existing_users
  pipeline.workers: 1
  path.config: "/usr/share/logstash/pipeline/site_users.conf"

- pipeline.id: tournament_scores
  pipeline.workers: 1
  path.config: "/usr/share/logstash/pipeline/game_scores.conf"